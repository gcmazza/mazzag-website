# Mazza-G Consulting Services — website

Bootstrap commit only — this repo needed at least one commit on `main`
before a pull request could target it. The real site lives on an open
pull request; see it there, not here.
